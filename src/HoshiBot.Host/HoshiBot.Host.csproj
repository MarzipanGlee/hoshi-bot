<Project Sdk="Microsoft.NET.Sdk.Worker">

  <PropertyGroup>
    <UserSecretsId>dotnet-HoshiBot.Host-9456395d-60d3-4f58-b506-fba4e5f9e748</UserSecretsId>
    <!-- Silences CS9057 from OllamaSharp's source-generator (skipped, newer Roslyn than the SDK),
         which propagates here via the HoshiBot.Discord project reference. -->
    <NoWarn>$(NoWarn);CS9057</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!-- Command-metadata localizations for NetCord's JsonLocalizationsProvider (see Program.cs).
         Plain copied files (not embedded): the provider reads them from disk next to the app, and
         `dotnet publish` carries CopyToOutputDirectory items into the publish output, so the
         Docker image (docker/Dockerfile.host) picks them up without extra steps. -->
    <None Update="Localizations\*.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Hosting" />
    <PackageReference Include="Microsoft.Extensions.Http" />
    <PackageReference Include="NetCord.Hosting" />
    <PackageReference Include="NetCord.Hosting.Services" />
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
    <PackageReference Include="Quartz.Extensions.Hosting" />
    <PackageReference Include="Quartz.Serialization.SystemTextJson" />
    <PackageReference Include="Serilog.Extensions.Hosting" />
    <PackageReference Include="Serilog.Settings.Configuration" />
    <PackageReference Include="Serilog.Sinks.Console" />
    <PackageReference Include="Serilog.Sinks.File" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\HoshiBot.Discord\HoshiBot.Discord.csproj" />
    <ProjectReference Include="..\HoshiBot.Domain\HoshiBot.Domain.csproj" />
    <ProjectReference Include="..\HoshiBot.Data\HoshiBot.Data.csproj" />
  </ItemGroup>
</Project>
