@page "/manage/database/stfc-news-posts"
@inherits DbContextPageBase

<PageTitle>STFC News Posts</PageTitle>

<h1>STFC News Posts</h1>
<p>
    Read-only — Alliance Tournament / Infinite Incursions posts detected by
    <code>StfcNewsNotifyJob</code>, and the shared crowd-sourced date-confirmation state for
    each. For debugging.
</p>

<QuickGrid Class="table" Items="Context.StfcNewsPosts.OrderByDescending(p => p.Id)">
    <PropertyColumn Property="post => post.Id" Sortable="true" />
    <PropertyColumn Property="post => post.EventGroup" Sortable="true" />
    <PropertyColumn Property="post => post.Title" Sortable="true" />
    <PropertyColumn Property="post => post.Link" Sortable="true" />
    <PropertyColumn Property="post => post.DetectedAt" Sortable="true" />
    <PropertyColumn Property="post => post.SubmittedDate" Sortable="true" />
    <PropertyColumn Property="post => post.SubmittedByDiscordUserId" Sortable="true" />
    <PropertyColumn Property="post => post.RequiredConfirmations" Sortable="true" />
    <PropertyColumn Property="post => post.LastDisplayedConfirmationCount" Title="Displayed Confirmations" Sortable="true" />
    <PropertyColumn Property="post => post.ResolvedAt" Sortable="true" />
</QuickGrid>
