@page "/manage/database/pending-modal-inputs"
@inherits DbContextPageBase

<PageTitle>Pending Modal Inputs</PageTitle>

<h1>Pending Modal Inputs</h1>
<p>
    Read-only — draft field values from a failed modal submission, kept so a "Back" button can
    reopen it pre-filled. Swept away by <code>PendingModalInputSweepJob</code>. For debugging.
</p>

<QuickGrid Class="table" Items="Context.PendingModalInputs.OrderByDescending(i => i.Id)">
    <PropertyColumn Property="input => input.Id" Sortable="true" />
    <PropertyColumn Property="input => input.GuildId" Sortable="true" />
    <PropertyColumn Property="input => input.DiscordUserId" Sortable="true" />
    <PropertyColumn Property="input => input.Kind" Sortable="true" />
    <PropertyColumn Property="input => input.Field1" Sortable="true" />
    <PropertyColumn Property="input => input.Field2" Sortable="true" />
    <PropertyColumn Property="input => input.Field3" Sortable="true" />
    <PropertyColumn Property="input => input.Field4" Sortable="true" />
    <PropertyColumn Property="input => input.CreatedAt" Sortable="true" />
</QuickGrid>
